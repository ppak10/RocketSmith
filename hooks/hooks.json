{
  "hooks": {
    "SessionStart": [
      {
        "name": "rocketsmith-dep-check",
        "type": "command",
        "command": "${extensionPath}/hooks/check-deps.sh",
        "timeout": 10000
      }
    ]
  }
}
